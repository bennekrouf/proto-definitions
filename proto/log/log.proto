
syntax = "proto3";

package log;

service LogService {
    rpc StreamLogs(LogRequest) returns (stream LogReply);
    rpc SendLog(LogRequest) returns (LogReply); // New RPC method
}

message LogRequest {
    string log_message = 1;
}

message LogReply {
    string log_message = 1;
}

